// types/supabase.js — empty runtime stub for the generated declarations in the
// sibling supabase.d.ts (which tsc resolves in preference to this file). Exists so
// JSDoc `import('../types/supabase.js')` type annotations use a specifier that is
// BOTH a valid tsc module path and a real file on disk — the asset-links rail
// (tests/contract/asset-links.test.js) checks every relative specifier, including
// ones inside JSDoc comments, against the filesystem. Never imported at runtime.
export {};
