# Listings fresh-start purge — 2026-06-06 (UTC 00:34:46Z)

Operation: keep only listings whose **latest** reaction is `like`; delete every
other row in `public.listings`. All learned/training data preserved.

## Result
| Metric | Value |
|---|---|
| listings before | 670 |
| KEEP (latest reaction = like) | 29 |
| DELETED | 641 |
| listings after | 29 |

## Keep-rule
A listing is kept iff, among all its rows in `listing_reactions`, the reaction
with the most recent `created_at` is `like`. Ever-liked-but-later-rejected → NOT
kept (7 such conflicts were deleted; their reaction history incl. the likes
remains intact in `listing_reactions`).

## Backups (two layers — nothing lost)
1. **Full fidelity (incl. `raw_json`)** — server-side, API-private `backup` schema:
   - `backup.listings_purge_20260606t003446z` (641 rows)
   - `backup.listings_keep_20260606t003446z` (29 rows)
   Restore: `INSERT INTO public.listings SELECT * FROM backup.listings_purge_20260606t003446z;`
2. **Git-tracked (all columns except `raw_json`)**:
   - `backups/listings_purge_20260606T003446Z.json` (641 rows)
   - `backups/listings_keep_20260606T003446Z.json` (29 rows)

## Integrity — learned/training tables UNCHANGED (before → after)
- `listing_reactions`: 3631 → 3631
- `learned_preferences`: 1 row, `updated_at` 2026-06-05 23:57:33.23+00 (identical)
- `refinement_suggestions`: 51 → 51
- `refinement_runs`: 2 → 2
- `sync_log`: 5099 → 5740 (+641 `listings`/`claude`/`delete` audit rows, one per id)

## Daily ingest (no code changed)
- Upsert is full-set on `rightmove_id` (`on_conflict=rightmove_id`, merge-duplicates);
  re-encounters preserve `first_seen` + `price_history`; reactions untouched.
- Out-of-scope filtered by **geofence** (`withinGeofence`, coordinate distance to
  nearest active village), not by outcode; geofence-fail rows are dropped pre-upsert.
- **Blind spot:** a normal daily run sets `maxDaysSinceAdded=3`, so it only fetches
  listings *added to Rightmove in the last 3 days*. After a fresh start, still-live
  older listings will NOT return via daily runs.
  **Fix (built-in, operational, no code change):** run one foundation pull
  `FOUNDATION_MODE=1` (omits `maxDaysSinceAdded` → fetches the full current in-scope
  live set, all ages). Daily 3-day window maintains thereafter.

## Scoring / refinement
- Review queue is scored client-side by `assets/js/listings/fit.js::scoreListingFit()`
  using `learned_preferences.derived` weights — fresh on every load, so the queue
  auto-orders best-first once repopulated (no stale/materialised score to rebuild).
- Existing engine: 51 `forming` suggestions, **0 actionable** (runs_qualified=0, lift≈1.0
  fails the ≥1.6 gate at the ~94% base reject rate). Persisting a fresh run requires the
  real engine `tools/refinement-run.mjs` (needs Supabase service creds / CI) — not
  hand-written here to avoid desyncing the engine's persistence/sticky-status invariants.
- Suggestions derived from the live learned model are surfaced in chat for review (not auto-applied).
