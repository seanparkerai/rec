# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Temp content files used by tools/insert-content.mjs (splice then delete)
*.tmp.md
*.tmp.html
*.tmp.json
/tmp-content/

# Node (only if tooling is added later; no build step today)
node_modules/
npm-debug.log*

# Local server / misc
.cache/

.tmp/

# Sensitive user-state data — lives in Supabase only
data/profile.json
data/finances.json
data/investments.json
data/goals.json
data/contacts.json
data/criteria.json
data/imports/trading212-history.json

# Local safety backups (never commit)
data/_backup/
/backups/

# Stryker mutation testing (opt-in, step 4.10)
.stryker-tmp/
.stryker-tmp-finance/
reports/
