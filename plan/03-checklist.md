# Living checklist — §9 the resumable backlog spine

> Split from `fable_refactor.md` (2026-07-01, content unchanged). Directory: [`plan/README.md`](README.md).

## 9. Living program checklist (Fable maintains this)

> This checklist is the spine the Session Mandate refers to. During the Plan-Mode overhaul, replace
> this stub with the agreed, sequenced backlog of **atomic steps** — each small enough to complete,
> test, commit, and tick in a single short working spell (§3.1). Tick + commit after every merged step
> so any credit-limited or cold session resumes from the exact next unticked line. Record baseline
> metrics here (test count/runtime, lint violations, segment coverage) so progress is measured, not
> asserted.

**Phase 0 — Onboarding (all in Plan Mode; ends at owner approval)**
- [x] Plan Mode entered; session confirmed read-only until approval (Session Mandate). *(2026-07-01)*
- [x] Full-codebase scan complete (§2); inventories verified/corrected — dated "§2 scan corrections"
      sections appended to `segments/10.{1,4,5,6,8,9,10}` + drift summary in `plan/README.md`. *(2026-07-01)*
- [x] Harness green at baseline; baseline metrics recorded: **783 pass / 0 fail** (+ sync 16/0/3
      online-skips); 67 test files; responsive-lint baseline 7 entries; 145 JS / 53 CSS / 28 tools /
      14 page surfaces. *(2026-07-01)*
- [x] Supabase schema + RLS confirmed via MCP: **33 tables, all RLS-enabled**. Snapshot NOT fresh —
      drift found (`profile` 06-21, `shortlist` 06-28, `learned_preferences` 07-01, `areas` 06-28 vs
      older high-water marks); reconcile per §18.2 at the first data-session start. *(2026-07-01)*
- [x] **Owner-directed insertion (2026-07-01): split `fable_refactor.md` into `plan/`** — granular
      files + directory index; merged to `main` (`91cec46`).
- [x] Global Q&A intake (§7.1) complete; answers recorded in `04-program.md` §1. *(2026-07-01)*
- [x] Guard-rail audit (§4.5) delivered (`04-program.md` §4); gates pre-authorised via the plan (owner, §7 intake). *(2026-07-01)*
- [x] New test standard (§5.2) designed (`04-program.md` §5); owner signed off the full blueprint incl. test-only devDependencies. *(2026-07-01)*
- [x] "State of the system" note delivered with top-3 targets (session note, 2026-07-01; summary in `plan/README.md` known-drift + `04-program.md`).
- [x] The plan (now `plan/`) overhauled into the final step-by-step program + atomic backlog (`b1ea902`). *(2026-07-01)*
- [x] Plan presented and **owner-approved** (2026-07-01) — execution unlocked.
- [x] On approval: overhauled plan committed *before* any product code moves (`b1ea902` + close-out ticks).


---

> **Backlog authored 2026-07-01 from the §2 scan + §7 intake (decisions in [`04-program.md`](04-program.md)).**
> Sequencing: the 2026-07-01 ⭐ TOP PRIORITY DIRECTIVE supersedes the 2026-06-16 "front-end first"
> order — **listings pipeline first (after the Phase-1 net core), mobile-first UI second.**
> Rules: one step = one commit = one tick, merged to `main`, harness green (§3). Phases 1–2 are
> atomic now; Phases 3–10 are expanded to atomic granularity just-in-time when their block starts
> (§0.2 mode-2 decision, `04-program.md` §1). ⚙ = flagged owner action.

**Phase 1 — The net, first: new test-harness core (§5; strangler — old runner stays green throughout)**
- [x] 1.1 devDependencies added (jsdom ^29.1.1, happy-dom ^20.10.6) + package-lock.json; Pages deploy verified unaffected (uploads a fresh checkout, node_modules gitignored). *(2026-07-01)*
- [x] 1.2 `tools/run-all-tests.mjs` stood up beside the old runner (`npm run test:new`): dynamic tier discovery, same `register()` contract (suites port by moving), `--tier` filter, per-tier summaries, lint + sync suite on full runs with online-skips reported as unrun. *(2026-07-01)*
- [x] 1.3 Fixtures centralised in `tests/fixtures.mjs` (memoised; both runners consume it) + `tests/mocks/supabase-client.js` (chainable fixture-backed stub, self-tested by the first unit-tier suite, 6/6). *(2026-07-01)*
- [x] 1.4 Batch A ported: 9 finance suites → `tests/unit/`, 2 → `tests/characterization/`; legacy runner + browser harness re-pointed; legacy 783/0, new harness 142/142. *(2026-07-01)*
- [x] 1.5 Batch B ported: 20 listings-pipeline suites → `tests/unit/` (incl. fetch/purge/backfill tool tests); legacy 783/0, new harness 365/365. *(2026-07-01)*
- [x] 1.6 Batch C ported: 11 intelligence-engine suites → `tests/unit/`. *(2026-07-01)*
- [x] 1.7 Batch D ported: 10 contract suites → `tests/contract/` (root-path + dynamic-import fixes; live docs re-pointed to the tiered paths; supabase-sync stays a spawned suite by design). Legacy 783/0; new 649/649. *(2026-07-01)*
- [x] 1.8 Remaining 14 suites ported (11 → unit, 3 → characterization); only the spawned `supabase-sync.test.js` stays at root by design. Porting complete: 67 suite files across tiers; legacy 783/0, new harness 789/789. *(2026-07-01)*
- [x] 1.9 DOM tier live: pure shell mechanics extracted to `assets/js/shell-core.js` (DI-friendly; `components.js` now the thin bootstrapper, behaviour preserved), first `tests/pages/shell.test.js` renders the real partials in jsdom (injection, nav resolution, active-state incl. the characterized 3-link home behaviour, failure fallback, theming); runner fixed to drain async tests. New 795/795; legacy 783/0. *(2026-07-01)*
- [x] 1.10 Semantic lint v2: baseline is now a justified RATCHETING allow-list ({count, reason} per fingerprint; new-by-identity always fails; stale allowances fail until `--tighten-baseline` locks the fix in; `--write-baseline` resnapshot removed as a laundering hazard). 7 entries ported with recorded reasons; both runners assert regressions+stale. *(2026-07-01)*
- [x] 1.11 RLS rail live: `tools/check-rls.mjs` (psql over SUPABASE_DB_URL; fails CI on any public table without RLS; honest SKIPPED without the secret) + 5 contract tests pinning its decision logic + `ci.yml` step (§16 rail phase, pre-authorised). Activates fully at owner step 2.16. *(2026-07-01)*
- [x] 1.12 Tier-0 type-check live with a RATCHETING scope (deviation recorded: the planned listings/storage/finances start = 90+ errors through the untyped import graph — whole-graph typing stays the Phase-9 R1/R4 job; scope starts clean at shell-core/config/dom/format, fixed via JSDoc-only annotations, and may only GROW as phases rebuild modules). `npm run typecheck` + Tier-0 step in the new harness. New 801/801; legacy 788/0. *(2026-07-01)*
- [x] 1.13 CUT OVER: `npm test` / `ci.yml` / `pages.yml` / CLAUDE.md §6 / DESIGN.md / live docs + skills all point at `tools/run-all-tests.mjs`; the legacy runner is a thin forwarder (deleted at the Phase-10 leanness sweep, refinement of "archive" so every documented command keeps working). **Phase 1 complete — re-baseline: 801/801 (598 unit + 120 contract + 69 characterization + 6 pages + Tier-0 + lint + sync 16/0/3-skip) across 69 tier-discovered suites, ~5s offline; devDeps jsdom/happy-dom/typescript.** *(2026-07-01)*

**Phase 2 — ⭐ FLAGSHIP: listings pipeline, DB-canonical rework (§10.4/§10.5/§10.9 + `logs/2026-07-01-listings-m2m.md` §5)**

*2.A — Pin current behaviour (before any change)*
- [x] 2.1 `withinGeofence()` characterized: existing unit coverage audited (petals/tiebreak/corroboration/membership already pinned); new `tests/characterization/geofence-pipeline.test.js` pins the subtle contracts the rework leans on — km-sorted membership, primary-position independence, INCLUSIVE buffer boundary, exact mi conversion, sectoral membership, mixed-universe resolution, determinism (7 tests). 808/808. *(2026-07-01)*
- [x] 2.2 Feed contract pinned at the INTEGRATION tier: the REAL `getListings()` runs under Node against the fixture mock via a new `core.js` test seam (`__REC_TEST_SB__` + `_resetStorageForTests`, extend-only §16 change) — membership scoping, Problem A (membership beats primary), Problem B (origin exclusion), paused-link exclusion, `geofence_pass` false/null/reveal semantics, status filter, ordering, membership attachment, empty-scope short-circuit (7 tests). Runner now executes tests sequentially (integration suites share process state). 815/815. *(2026-07-01)*
- [x] 2.3 Composed target-pipeline golden-master (`tests/characterization/fetch-targets.test.js`): radius-tuning → demand-gating → cluster targets → dedupe over a fixture universe with exact serialized output (tight clusters, coarse whole-outcode fallback, shared-identifier merge, origin absence), tuning/exploration reshaping, the cluster≤outcode cost invariant, and partial demand pruning. 2.A complete. 819/819. *(2026-07-01)*

*2.B — One geofence universe*
- [x] 2.4 `tools/lib/geofence-universe.mjs` created: pure `buildUniverse()` core (inclusion = coords AND (active OR linked); tuning applied; outcode grouping) + `toVillage` (lifted) + `applyRadiusTuning` (moved from the fetcher, re-exported for compat) + DB REST edge and repo materialised-view edge; 6 unit tests incl. stubbed-REST composition and a real-repo smoke (175 villages / 18 outcodes today). 825/825. *(2026-07-01)*
- [x] 2.5 Fetcher migrated: `loadOutcodeMap` is now a thin call to the canonical `loadUniverseFromRepo()` (divergent parsing deleted; tuning overlay already shared since 2.4; live stub merge unchanged); golden-master byte-identical. `import-apify-runs` consumes the same export, so it rides along until its own 2.8 migration. 825/825. *(2026-07-01)*
- [x] 2.6 `backfill-listing-areas.mjs` migrated: local `toVillage` deleted (re-exported from the lib for the --villages path + tests); `restLoadVillages` is now the canonical DB edge; stale `applyRadiusTuning` cross-import from the fetcher removed. 825/825. *(2026-07-01)*
- [x] 2.7 `backfill-geofence` + `radius-tune` migrated onto the canonical loader; new `includeDisabled` option preserves radius-tune's full-catalog geometry (historic reactions in disabled areas keep bearings) with a unit test; all three divergent loaders are now DEAD. 826/826. *(2026-07-01)*
- [x] 2.8 ONE MATCHER: importer accept-gate unified onto `withinGeofence` (in-buffer accepted; near-miss ≤20 km stored-but-hidden preserving includeOutOfArea; wrong-region/coordinate-less dropped); `matchListingToArea` deleted from `listings-normalise.mjs` (+ its 3 tests); dead `assignArea` deleted from the fetcher; new contract rail `tests/contract/one-matcher.test.js` scans tool sources so the retired matchers can never return (it caught a residual import during the change). 2.B complete — one universe, one matcher. 825/825. *(2026-07-01)*

*2.C — One membership truth*
- [x] 2.9 MIGRATION LIVE (`derived_primary_from_listing_areas`): `uniq_listing_areas_primary` partial unique index (multi-primary structurally impossible) + `replace_listing_areas` v2 (validates exactly-one-primary at the boundary; DERIVES `listings.area_id` from the junction in the same transaction). Pre-gate 0 mismatches/0 multi-primary; synthetic 4-assertion live verification passed + cleaned; real data untouched (1067/4028). Applied via execute_sql WITH history insert (apply_migration approval stream down — disclosed); mirror + DATA_MODEL + SUPABASE_SYNC updated incl. the §18.3 parity SQL. FOUND: 4 live geofence-passing listings with zero membership (feed-invisible today) → 2.11 repair queue. 825/825. *(2026-07-01)*
- [x] 2.10 Writers simplified to write-membership-once: `membershipFor` is verdict-driven (stored-column alignment + `primaryFix` deleted; `primaryDrift` kept as a stale-geofence-fields signal pairing with `backfill-geofence`); raw `emitSql`/`--emit-sql` write path deleted (would bypass the deriving RPC — RPC is the ONLY membership writer; `--from-file` is report-only); `restReplace` fix-upsert dropped. Fetcher/importer keep setting the identical initial `area_id` on insert (RPC re-derives). Tests rewritten to the new contract. 825/825. *(2026-07-01)*
- [x] 2.11a HOTFIX (2026-07-01): the 3 repairable feed-invisible listings (173588246, 90359223, 90374985) recomputed through the canonical machinery (buildUniverse + membershipFor, DB-edge-identical universe incl. stubs+tuning) and written via the deriving RPC — 13 membership rows, parity 0. 174197870 is correctly OUT of every buffer under today's tuned radii (stubbington 0.76 mi): its stale geofence_pass=true is a FIELD refresh for the sweep.
- [ ] 2.11b Full membership + geofence-field sweep runs in CI via the 2.15 workflow (fields first via backfill-geofence, then membership via the RPC path), checksum-verified — a ~4,000-row bulk write belongs in CI, not MCP-transported SQL (log §5.8). Unblocked by ⚙ 2.16.

*2.D — One visibility predicate*
- [x] 2.12 Migration LIVE (`household_feed_rpc`): `household_feed(p_household_id, …)` SECURITY DEFINER RPC — membership ∩ non-origin active areas ∩ curated-disable ∩ `geofence_pass` ∩ baseline (classify.js constants + \y-translated type regexes, anti-drift-pinned by `tests/contract/household-feed.test.js` over the `supabase/archive/schema-household-feed.sql` mirror), ordered + paged, `areas` jsonb attached; fixture reference impl `tests/mocks/household-feed-rpc.js` (12 contract tests). Live verify: exact set parity with the reference predicate (392=392, 0 missing/extra/doubled), Shedfield visible, baseline hides only the ever-liked £435k row (already decided-suppressed → zero visible-row change); guard verified anon/member/non-member. Applied via execute_sql + history insert (apply_migration approval stream down — disclosed, as 2.9). 837/837. *(2026-07-01)*
- [x] 2.13 `storage/listings/feed.js` pointed at the RPC (rail phase, pre-authorised): scoped read = ONE `household_feed` call (paged; names resolved client-side); the client id-list `.in()`, local origin filter, catalog-disable filter and double geofence gate all retired; unscoped path (saved view/signed-out) unchanged. Feed integration contract (2.2) green THROUGH the cutover — the mock now serves the RPC from the 2.12 reference impl; the sync-suite source-scan re-pinned to the new one-predicate contract (retired plumbing can't return). 837/837. *(2026-07-01)*
- [x] 2.14 Live acceptance vs the RPC (all §3 invariants of the 2026-07-01 log hold): Whiteley the sole origin, 224 origin-only listings hidden with **0 leaking** into the feed; Shedfield 90328152 visible via waltham-chase-so32 membership despite its wickham primary stamp (Problem A); owner feed 392 rows (exact set-parity vs the reference predicate proven at 2.12; 395→392 is listings churn); all 3 households return sane feeds (97/392/431). 2.D complete — one visibility predicate, live and consumed. *(2026-07-01)*

*2.E — Freshness, dedupe, origins, hygiene*
- [x] 2.15 Re-membership sweep workflow (`.github/workflows/remembership.yml`): fields first (`backfill-geofence`, now reading the canonical DB universe in REST mode — the repo edge would flip stub-area listings false + ignore tuning) → membership via the deriving RPC tool → psql invariants that fail red (§18.3 primary parity + no feed-invisible listings; orphan junction rows surfaced as info for 2.18). Triggers: dispatch (dry_run input) + after each successful radius-tune + weekly Sun 05:45 UTC; honest no-op warning until ⚙ 2.16 secrets exist. Contract-pinned (`tests/contract/remembership-workflow.test.js`: fields-first order, RPC-only writer, red invariants, DB-universe edge). 840/840. *(2026-07-01)*
- [ ] 2.16 ⚙ Owner adds `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` / `SUPABASE_DB_URL` repo secrets (unlocks 2.15, refinement-run.yml, CI backfills). **Owner deferral recorded 2026-07-02: skipped for now** — engine cadence + re-membership stay manual/MCP-run; do not re-nag, revisit only when the owner raises it.
- [x] 2.17 Dedupe audit complete (`tests/characterization/dedupe-audit.test.js`): relist/decided/saved collapses + two-membership single-row were already pinned (unit + 2.12 contract, live 0 doubled); newly pinned — mergePriceHistory edges (no-change, first-sight, legacy scalar diff, null-price re-fetch, A→B→A), within-run first-wins dedupe, and a writer-parity source rail (UPSERT on rightmove_id + never-blank preservation + existing-row SELECT shape). FOUND+FIXED: the importer clobbered held image_url/price on re-import (fetcher preserved, importer spread the raw row and didn't even SELECT image_url) — now fetcher-parity. Recorded accepted limits: price_history restarts on relist-under-new-id; coarse-address (null-fingerprint) pairs may both render (never-falsely-merge wins). 847/847. *(2026-07-01)*
- [x] 2.18 Listing lifecycle audit: status enum + writers + freeze-then-stale delisting path documented (DATA_MODEL §"Listing lifecycle"); FOUND+FIXED an undocumented `'new-build'` purge reason smuggled in by unrelated commit fc5574a — absent from PURGE_REASONS, no report bucket (crashed `main()` when reachable), contradicted the new-build-is-a-FLAG rule — deleted, and PURGE_REASONS pinned as the COMPLETE set; FIXED junction hygiene — purge now deletes each purged listing's `listing_areas` rows (no FK; live orphan count today: 0, crash-reachable rows today: 0). Purge needs no geofence universe by design (baseline/reaction/staleness only — noted; the checklist's "shared universe" expectation was stale). 849/849. *(2026-07-01)*
- [x] 2.19 Origin/target as first-class UI: every chip in the shared area picker (profile + areas/map — one component, both surfaces) carries an accessible "Home" toggle (`aria-pressed`, state in TEXT "(home)" + dashed reinforcement, §11-clean) writing the new extend-only `storage#setHouseholdAreaOrigin`; composition now selects/carries `_isOrigin`; status line explains the feed/commute consequence on every flip. Integration-tested via the core seam (write shape, household scoping, reversibility) + source pins; downstream behaviour already railed (RPC contract + fetcher demand gate). Replaces the one-off SQL seed as the way is_origin is managed. 853/853. *(2026-07-01)*
- [x] 2.20 Dead mechanics stripped (`excludeCuratedDisabled` + tests — production-dead since 2.13; backfill `sqlLiteral` — orphaned by 2.10; alive-with-callers verified for the survivors); docs reconciled (DATA_MODEL/SUPABASE_SYNC already current in-step; FETCH_SCHEDULE + remembership note; REPO_MAP + `plan/` row; tools/README + universe lib/backfills; INTELLIGENCE_RULES checked clean). **Flagship exit review written: `plan/logs/2026-07-01-flagship-exit.md`** — all four collapses DONE, §3 contract live-verified (nothing missing/doubled/leaked/from-home), weakness list #1–#9 dispositioned. Phase 2 remainder is owner-gated only: ⚙ 2.16 secrets → then 2.11b = one manual `remembership` run. 852/852 (one test left with its dead helper). *(2026-07-01)*

**Phase 3 — Mobile-first UI/UX overhaul (IA rethink, page-by-page on existing foundations)** *(entered 2026-07-01; 3.2–3.3 are IA-independent and proceed while ⚙ 3.1 review is pending; 3.4–3.9 expand to atomic once the review lands)*
- [x] 3.1 ⚙ Owner design review COMPLETE (2026-07-02, via AskUserQuestion + one reconciliation): **burger-only nav (NO tab bar), drawer re-ordered daily-loop-first** (Home·Properties·Areas·Money·Ask lead); **three property pages KEPT, all driving from ONE overhauled primary design** (owner: rejected page's cleanliness is the reference bar; the shared design must be "absolutely the most optimal"); **verdict-led Home approved** with a hard at-a-glance mobile/tablet constraint; standing latitude granted to implement best/latest design practice and actively overhaul anywhere it improves. Decisions recorded in [`05-ia-proposal.md`](05-ia-proposal.md) §5. 3.4–3.9 expanded below honouring them. *(2026-07-02)*
- [x] 3.2 C1: OKLCH/`color-mix` fallbacks live in `tokens.css` (rail phase, named + pre-authorised). Mechanism corrected from the validation note: custom properties fail at USAGE time, so "plain value declared first" can't work — an `@supports not (color: oklch(…))` override block carries sRGB approximations for every modern colour token, in all three theme scopes (light, forced dark, system-auto dark). New contract rail `tests/contract/tokens-fallback.test.js` (3 tests): block exists + is legacy-safe, every oklch/color-mix token has a fallback, dark fallbacks cover both dark paths. DESIGN.md colour contract updated. 855/855. *(2026-07-01)*
- [x] 3.3a Shell resilience: `injectIncludes` now races every partial fetch against a 4s timeout (a hung CDN can no longer stall `shell:ready` — pages went dark forever before), a failed/timed-out header degrades to a minimal brand-only bar (never chrome-less; new token-driven `components/shell-fallback.css`, base.css append), and every failure surfaces a visible `role="alert"` strip naming WHAT failed with a same-URL refresh link (errors were console-only). Two new jsdom page tests (hung-fetch timeout path; 404-footer path). 857/857. *(2026-07-01)*
- [x] 3.3b Shell bootstrap extraction: theme / nav-drawer / header-user wiring moved verbatim-in-behaviour into `assets/js/shell/{theme,nav-drawer,header-user}.js` (DI collaborators; header-user no longer imports storage — components.js injects it); `components.js` is now the thin sequencer (~55 lines), script entry path + exports unchanged, zero external importers verified. New `tests/pages/shell-wiring.test.js` (4 tests over the REAL partials): theme click-flip persists + mirrors `aria-pressed`; burger/close/nav-link drawer choreography with `aria-expanded` (dialog internals shimmed — jsdom has none; our wiring is the unit); admin reveal + sign-out via injected auth; signed-out quiet path. 861/861. *(2026-07-01)*
*(3.4–3.6 expanded 2026-07-02 from the ⚙ 3.1 decisions; 3.7–3.9 expand as reached)*
- [x] 3.4a Drawer re-ordered daily-loop-first per decision 1: Home · Listings · Areas · Finances · Ask, then Saved · Rejected · Trends · Journey · Profile (+ latent admin). Pure reorder of `components/nav.html`; new jsdom pin locks the leading five + total count so a rebuild can't scramble it silently. 896/896. *(2026-07-02)*
- [x] 3.4b THE shared property design system BUILT (strangler — beside the old, consumed from 3.4c): `assets/js/listings/property-card.js` (pure slot-based builder: shared media well with lazy/no-referrer/monogram-fallback + labelled dossier affordance, verdict-dot + mono price headline, title/place/meta; caller slots for tags/details/badge/thumb-zone actions; `propertyTitle/Place/Meta` helpers) + `components/property-card.css` (Linear-dense, hairline rows per the rejected-page register, tokens-only, container-query sizing incl. compact register + 560cqi adaptation, ≥44px action zone) imported from base.css. New jsdom suite pins anatomy, a11y contract, fallback behaviour, slot composition, and the no-inline-style rule. 900/900. *(2026-07-02)*
- [x] 3.4c Browse rebuilt on the shared system: `buildRow` is now a thin composition over `buildPropertyCard` (chips/membership/why/reactions/status/links into the tags/details/actions slots; reviewed → shared badge); the feed is the shared `.prop-list` hairline register (`role="list"` div, `role="listitem"` cards; empty states are static markup so the list only ever holds items); controls + reveal toggles moved into a native `<dialog class="filter-sheet">` (modal sheet on phones behind a pill-mirroring trigger row, inline card ≥768px — new DI-testable `listings/filter-sheet.js`, reused at 3.4d). Storage untouched — feed contract/paging (`tests/integration/feed-scope.test.js`) green through the cutover. New `tests/pages/listings-browse.test.js` (5 tests). Legacy `.listing-card` CSS stays for the deck/Saved until 3.4d deletes it last. 905/905. *(2026-07-02)*
- [x] 3.4d Saved + Rejected rebuilt as thin compositions of the same system (two commits: compositions, then styles-die-last). Saved: pure builder extracted to `page-saved-listings/card.js` (♥ Liked badge, positives chips as details, collapsed editor + rating + Rightmove in the actions slot), controls into the shared filter sheet, `.prop-list` register + static empty state (coordinator 257→180 lines). Rejected: the table became the compact register via `page-rejected/row.js` (verdict badge, Actioned date on the data line, area place fallback); search/50-per-page pagination/empty survive. Deleted last: `.listing-list`/`.listing-card*`/`.listing-media*` blocks, the rejected table/thumb/verdict CSS, dead media-link hover rules; return-highlight renamed to `prop-card--returned` with `scroll-margin-top` now on the shared card. New `tests/pages/saved-rejected.test.js` (4 tests). 909/909. *(2026-07-02)*
*(3.5 expanded 2026-07-02 on entry — reality check: the dossier already ships most of the original wishlist — eager hero + lazy thumbs + native-`<dialog>` lightbox with keyboard stepping, the OPEN "why this verdict", price history, floor plan, full m2m membership section, rail actions. "srcset" is DROPPED (reality wins): Rightmove media URLs are single-size; synthesizing variants would 404. Remaining gaps:)*
- [x] 3.5a Lint-baseline ratchet the allow-list flagged for this phase: `page-listings/progress.js` `.style.width` ×2 → the sanctioned `--fill-pct` `setProperty` idiom; `page-property/sections.js` `.style.cursor` → a CSS rule; baseline tightened 6→4 fingerprints (the survivors: two permanent viewTransitionName sanctions, the SVG strokeDashoffset animation, and section-deposit's width — flagged for the 3.8 finances rebuild). *(2026-07-02)*
- [x] 3.5b Membership section → "why am I seeing this" chips (⚙ 3.1 wireframe): the dossier's m2m list rows became linked `chip-grid` chips (nearest-first, mono distance, primary marked in text, each stepping into its area dossier); section headline now asks/answers the question directly. New `tests/pages/dossier.test.js`. 911/911. *(2026-07-02)*
- [x] 3.5c Thumb-zone actions FIXED (reality-wins deviation: the sticky-bottom card already existed but was trapped in the short `.dossier__rail` containing block, so it never rode over the dossier body; and it's CSS-only, so no jsdom pin — DESIGN.md §4 hand-off instead). Mobile-first the rail now DISSOLVES (`display: contents`) so the whole dossier is the sticky containing block — the "Your call" bar genuinely docks at the thumb for the full scroll (safe-area padding + ≥44px targets already in place); ≥1024 the rail re-forms as the sticky right column, unchanged. 911/911. *(2026-07-02)*
- [x] 3.5d Long-scroll relief: Description + Price history fold behind a shared `collapsibleSection` shell — native `<details>` OPEN by default (nothing ever hidden), the section label IS the summary with a rotating `›` cue (text-paired, motion-safe, ≥44px target, focus-visible). Pinned in `tests/pages/dossier.test.js`. **3.5 dossier block complete.** 912/912. *(2026-07-02)*
*(3.6 expanded 2026-07-02 on entry — reality check: the "new-since-last-visit → Properties" element already exists as the review-count strip under the lede (it runs the exact Browse partition pipeline, so it can never drift from the feed); the readiness verdict exists but sits buried in a mid-page tile; the bento already has band labels — wrong ranks (Goal progress · Affordability · Search · Next steps) and a filler page-head lead line above the fold.)*
- [x] 3.6a Verdict strip: the lede LEADS with the deposit-readiness verdict — pure `depositVerdict()` in tile-lede.js (same maths as the readiness tile's headline) fed by goals (page-home fetches once); prose demotes to a quiet second line via `page-lede--verdict` (cold-start with no target keeps prose-first unchanged); the redundant page-head filler lead cut for fold space. Review strip stays directly beneath as the → Properties action. New `tests/pages/home.test.js`. *(2026-07-02, shipped with 3.6b in one commit — same file, same pin suite)*
- [x] 3.6b Bands re-ranked **Act · Money · Track** (decision 3): Act = readiness · journey · ask; Money = deposit · afford · scenarios · flow · deposit-risk · networth · fan · seasoning; Track = shortlist · criteria. Pure markup reorder — all 13 tiles survive (pinned: band order + per-band membership + the cut-nothing count). 915/915. *(2026-07-02)*
- [x] 3.6c At-a-glance pass done: phones tighten the strip (page-head hugs the lede via `page-head--tight`, lede margins/padding/gap drop a step, meta values `text-base`) so verdict + review count land in the first viewport; the magazine spacing returns at 768. Band audit CLEAN — the only 720px thresholds are `@container` tile queries (the sanctioned §6.6 mechanism, not viewport breakpoints); all viewport queries sit on the 480/768/1024/1280 canon. Owner waived per-step visual verification for the remainder of the refactor (2026-07-02) — outstanding polish folds into a post-refactor visual pass. **3.6 Home block complete.** 915/915. *(2026-07-02)*
*(3.7 expanded 2026-07-02 on entry — first slice; 3.7b–d re-verify against the live page when reached:)*
- [x] 3.7a Filter-sheet dedupe: the module promoted to the flat shared utility `assets/js/filter-sheet.js` (§19 — three consumers across two segments; listings/saved importers + test re-pointed) and the areas page's ORIGINAL inline script (the one 3.4c extracted from) deleted — replaced by `areas/filter-sheet-init.js` on the shared mechanism, keeping the areas describe(), the result-count footer mirror and the list-render refresh triggers. FIXED en route: the inline pills were `innerHTML` over raw search text (markup injection) — the shared module renders pills as TEXT (pinned in new `tests/pages/areas-filters.test.js`). 916/916. *(2026-07-02)*
- [x] 3.7b Map-first mobile layout, LEAN FORM (deviation recorded: the wireframe's draggable list sheet = custom gesture/inert machinery judged over-engineering for a zero-build page — same outcomes shipped instead; revisit at the post-refactor visual pass only if wanted). On phones the map now fills the viewport minus the sticky header (`calc(100dvh - var(--header-h) - var(--space-8))`, min 360px — was a 50svh half-screen) with a floating safe-area-aware "Areas list ↓" chip (≥44px, above Leaflet's z-panes) jumping to the `#directory` anchor (scroll-margin clears the header, §11); ≥768 the chip hides and the banded heights return unchanged (50svh / 60dvh@1024). Markup order pinned in `tests/pages/areas-filters.test.js`. 917/917. *(2026-07-02)*
- [x] 3.7c DECIDED: **KEEP LEAFLET** — the MapLibre GL + PMTiles swap is declined (ADR in [`04-program.md`](04-program.md) §5b; supersedes the old `docs/CHECKLIST.md` 8B queue item). Weighed per the improving-only latitude: the metric geofence circle (`L.circle` in metres) is the map's load-bearing visual and has no native GL equivalent; Geoman's zone draw/edit/snap/persist path (real user data) has no free MapLibre parity (maplibre-gl-draw is stale mapbox-compat, terra-draw = new API + dep); the dep ledger worsens (2 CDN deps ~55 KB gz → 3–4 deps ~230 KB+ gz, plus a self-hosted PMTiles binary or still-remote vector styles) on a zero-build page; and 3.7b just stabilised this surface. The genuine queued map improvements (Geoman toolbar a11y labels, coord-quality cues) are stack-independent and fold into 3.9/the post-refactor visual pass. Revisit trigger: CARTO raster retirement, or the visual pass judging raster tiles below the design bar. *(2026-07-02)*
- [x] 3.7d Area-detail pass DONE (reality check first: the wireframe's two headline deliverables were ALREADY LIVE — the mini-TOC is sticky at both registers (`components/toc.css`: blurred chip row pinned under the header on phones, vertical spine ≥1024) with IntersectionObserver `aria-current` highlighting, and the matched-price verdict strip (price + monthly P&I + fit dot) sits directly under the page head). Shipped: the four colour tail sections (Things to do · Places to eat · Pros & cons · Who it suits) now share the 3.5d `dossier-fold` shell — static-markup-only change (native `<details>` OPEN by default, the editorial h2 IS the summary, render-slot ids + TOC anchors untouched so the JS and anchor jumps are unchanged); decision content (overview/amenities/schools/transport/prices) stays unfolded. New `tests/pages/area-detail.test.js` pins above-fold order, TOC↔section anchor parity, the fold contract, and no-inline-styles. NOTED for 3.9: `area-detail.html` + `profile.html` carry near-identical inline TOC-observer scripts — extract to one shared module at the profile pass. 921/921. *(2026-07-02)*
*(3.8 expanded 2026-07-02 on entry — reality check: the page is already staged (Today/Investments/Purchase sticky `finance-toc` + scrollspy, stage heads), the calculators already share canonical inputs via the unified afford widget (DESIGN.md §5 rule 2 satisfied) with the one-time tables behind `collapsible-card` details, and every inline SVG chart already ships `viewBox` + `width:100%/height:auto` + short-viewport `svh` caps. Remaining gaps → sub-steps below. Overlap note re-checked: 5.9+5.10 are absorbed here as 3.8c/3.8d; 5.11 (data-flow hygiene) is NOT page-rebuild work and STAYS in Phase 5; 5.8's chart-module split also stays in Phase 5 — `section-v3-charts.js` is 334 lines, under the §19 ~400 norm, so re-decide it there.)*
- [x] 3.8a Verdict-led lede: the page now LEADS with "can we buy at the target, and what's the headroom?" — new `finances/section-verdict.js`, pure composition over the pinned `assessAffordability` surface at the goal price (headline + fit dot + Target/£mo/Max-at-4.5× mono row; §3.10b-safe by construction — no new maths, every figure already renders in the afford widget at the same price); strip sits above the topic nav, cold-start (no target) stays hidden, and the filler lead line it replaces is cut (3.6c precedent). CSS mirrors the area-verdict register in the page's Linear-dense voice. New `tests/pages/finances.test.js` pins markup order + the composition contract (headline ≡ calculator headline). *(2026-07-02)*
- [x] 3.8b section-deposit lint debt DONE: the deposit progress bar's `.style.width` → the sanctioned `--fill-pct` `setProperty` idiom (3.5a precedent; consumer-scoped CSS rule, the generic `.progress > span` default untouched); allow-list tightened 4→3 (survivors: two permanent viewTransitionName sanctions + the SVG strokeDashoffset animation). 923/923. *(2026-07-02)*
- [x] 3.8c (absorbs 5.9) Chart sizing DONE: `.chart-tall` DELETED (a stale duplicate that fixed the CARD to the same clamp as its inner wrap — a height its heading+caption could never fit; the four cards drop the class, `.chart-wrap/--tall` is the one mechanism); width-keyed heights → viewport-HEIGHT idiom (`50vw`→`32dvh` tall, shared dashboard `.chart-wrap` `45vw`→`30dvh` — same idiom swap, home rides along; the max-height:600px `svh` blocks still own short viewports); flow-bar verified ≤320px-safe (`width:100%`, fixed 2.5rem). Source rail in the finances suite: no chart height keyed to `vw` can return. Lint green, no new allowances. 924/924. *(2026-07-02)*
- [x] 3.8d (absorbs 5.10) Chart a11y DONE (deviation recorded, reality wins: the SVG renderers `replaceChildren()` on every draw, so static in-SVG `<title>/<desc>` would be wiped on first render — the durable form shipped instead: `role="img"` + the existing `aria-label` name + `aria-describedby` → the LIVE verdict-bearing `.chart-caption`, which also serves DESIGN.md rule 4 better than static boilerplate). All 12 chart surfaces covered (7 inline SVGs incl. both flow bars + waterfall; 5 Chart.js canvases named, 4 caption-described). Pinned: every main svg/canvas has role=img + a name, every describedby resolves, every SVG is caption-described. 925/925. *(2026-07-02)*
- [x] 3.8e Phase-3.8 exit DONE: ⚙ 3.1 wireframe re-verified line-by-line — "verdict first" shipped at 3.8a; "calculators as collapsible stages" was ALREADY LIVE (Today/Investments/Purchase staged `finance-toc` + scrollspy, unified shared-input afford widget per DESIGN.md rule 2, one-time tables behind `collapsible-card` details) and is recorded as such rather than rebuilt; "container-query charts + SVG title/desc/aria" landed as 3.8c/3.8d (with the recorded caption-description deviation). Phase-5 5.9/5.10 ticked as absorbed; docs checked clean (no stale sizing claims). **3.8 finances block complete.** Re-baseline: 925/925 (pages tier: 10 suites / 42 tests); lint baseline 3 justified fingerprints. *(2026-07-02)*
*(3.9 expanded 2026-07-02 on entry — first slice shipped; 3.9b–e re-verify against the live pages when reached, per the 3.5/3.6/3.8 reality-check pattern:)*
- [x] 3.9a One shared TOC scrollspy: the near-identical inline IntersectionObserver scripts on `area-detail.html` + `profile.html` (flagged at 3.7d) are DELETED, replaced by `assets/js/toc-observer.js` — one mechanism for both editorial spines (self-runs on import for `.area-toc`/`.about-toc`; export is the test seam), and profile GAINS the mobile active-pill auto-scroll it lacked (behaviour-improving unification — `components/toc.css` already styles both spines as chip rows). New `tests/pages/toc-observer.test.js` (IO shim — our wiring is the unit): module loaded + inline scripts gone on both pages, every TOC-linked section observed, aria-current follows the viewport with exactly one current link. 928/928. *(2026-07-02)*
- [x] 3.9b Profile pass DONE as verify+pin (reality check: ALL THREE wireframe items already live — inline-edit field groups via the shared field engine + native `<dialog>` + save bar, the Areas section IS the shared picker (`mountAreaPicker` on `#areas-mount`), and the first-run banner ships hidden until real data; the spine rides the 3.9a shared scrollspy). Shipped: `tests/pages/profile.test.js` locks the wireframe contract (banner/picker/dialog/save-bar/no-inline-styles + spine↔section anchor parity in order) so a rebuild can't lose it silently. No markup change needed. 930/930. *(2026-07-02)*
- [x] 3.9c Journey pass DONE (reality check: vertical `ol.timeline` with current-phase/step marking, tick-modal → journey_progress sync, polite live region and native dialogs were all live; the one literal wireframe gap was "current stage PINNED"). Shipped: `.tl-progress` ("Where you are: <step>" + overall %) is now the sticky spine — pinned under the header on the finance-toc/area-toc register (blurred paper, hairline seam) while the long timeline scrolls; CSS-only. New `tests/pages/journey.test.js` pins the page contract (progress line leads the timeline, live region, dialogs, no inline styles) + the sticky mechanism at source. 932/932. *(2026-07-02)*
- [x] 3.9d Ask pass DONE as verify+pin (reality check: the wireframe was FULLY live — `ask.css` is explicitly iPhone-first with the chat column filling `100dvh`-minus-header, the composer `position:sticky; bottom:0` above the home indicator with safe-area insets and `svh`-capped input growth, and the Messages/Compose/History surfaces as native dialogs). Shipped: `tests/pages/ask.test.js` locks the page contract (transcript→composer order, composer is a real form, three native dialogs, no inline styles) + the pinning mechanism at source (sticky-bottom composer, dvh fill, no raw `vh`). No markup/CSS change needed. 934/934. *(2026-07-02)*
- [x] 3.9e Focus hardening DONE — two new lint rules + a real find FIXED. `r-focus-kill` (removing an outline without a same-block replacement fails; the two legitimate container-indicator cases — ask composer's `:focus-within` wrapper ring, the gallery's surviving global box-shadow — are allow-listed WITH reasons) and `r-focus-ring-as-outline` (the trap the sweep found: `--focus-ring` is a BOX-SHADOW value, so `outline: var(--focus-ring)` is invalid-at-computed-value and silently killed the global focus outline — **live ×29 across 9 CSS files, all fixed** to the base.css `2px solid var(--accent)` idiom, so those 29 controls now render a true outline + the global ring instead of box-shadow-only). DESIGN.md §2 documents the idiom + both rules. First-run regex backtracking hole in the replacement check found+fixed during build. 934/934. *(2026-07-02)*
- [x] 3.9e2 Contrast rail LIVE: `tests/contract/tokens-contrast.test.js` computes real WCAG ratios from the `tokens.css` oklch/color-mix ladder (full OKLab→linear-sRGB pipeline, scope-chained theme resolution, hue-arc-aware mix math, parser-sanity pins). Floors held both themes: ink 17.6/16.5, ink-muted 5.4/6.4, accent-ink 6.9/10.4 (≥4.5); accent-on-paper 4.4/8.4 (≥3, SC 1.4.11). **Two findings recorded as raise-only RATCHETS** for the post-refactor visual pass (token nudges = visible changes across every button/placeholder): `--accent-contrast` on accent **4.44** light (floor 4.4, target 4.5) and `--ink-subtle` on paper **2.85** both themes (floor 2.8, target 3.0+). **Real-browser smoke-tier verdict: NOT NOW** — the jsdom pages tier (14 suites/51 tests) + the owner's on-device checks cover the refactor; a Playwright tier adds CI weight without a driving need; revisit at the post-refactor visual pass where real-browser eyes are already owed. 936/936. *(2026-07-02)*
- [x] 3.9f **PHASE 3 COMPLETE** — exit review done: the ⚙ 3.1 wireframe table walked line-by-line and reconciled in [`05-ia-proposal.md`](05-ia-proposal.md) §6 (every row shipped or verified-live-and-pinned; all deviations dated: srcset drop, map lean form, KEEP-LEAFLET ADR, caption-described charts, Trends deferred-by-design to Phase 4); DESIGN.md re-baselined in-phase (focus-ring idiom, C1 fallback note); `plan/README.md` status block re-baselined for cold resumes; REPO_MAP deliberately list-free (no update owed). **Phase-3 re-baseline: 936/936 across 96 suites (pages tier 14 suites/51 tests — from 8/34 at entry); lint 5 justified fingerprints (2 focus + 3 style-assign) under 2 new a11y rules + the contrast rail; every page pinned by a DOM suite.** Outstanding visual polish + the two contrast ratchets fold into the post-refactor visual pass (owner waiver 2026-07-02). *(2026-07-02)*
- [x] 3.10 OWNER-DIRECTED (2026-07-02, post-phase, on-device screenshots): mobile prop-card register redesigned IMAGE-LED — on phones the photo now spans the card full-width (16/10 reserved box, zero-CLS lazy load, the whole photo is the dossier tap target) with the price stepped up to `--text-lg` and content stacked beneath; the row-on-hairlines register returns at container ≥560px; compact (Rejected & passed) keeps the small-thumb row at every width. ROOT CAUSE also fixed: Pico's `<article>` card chrome (background/shadow/radius/double padding — a DESIGN.md §3 ban, visible in the owner's screenshots) was never reset on `.prop-card` — now flattened, restoring the intended hairline register on all three surfaces. NAV: Saved/Rejected links verified NEVER REMOVED (drawer positions 6–7 since 3.4a); findability fixed — labels now mirror their page titles ("Saved listings", "Rejected & passed") and quiet hairline separators mark the drawer clusters. Both links + labels pinned in shell.test.js; the mobile register pinned at CSS source in property-card.test.js. 979/979. *(2026-07-02)*
- [x] 3.11 OWNER-DIRECTED (2026-07-02, post-phase, second on-device review; ticked retroactively 2026-07-02 — the commit `be076d9` shipped but the tick was lost to a session cut): Rightmove-app register — the cover photo leads at EVERY width (full-card 3:2; the ≥560px side-thumbnail fallback deleted); Browse + Saved flow 2-up ≥768 / 3-up ≥1280 via `.prop-list--grid` (sanctioned in DESIGN.md §1 as a content/media index; dense Rejected keeps compact rows); price-drop/New chips overlay the photo top-left (click-transparent, text stays in the a11y tree); below the photo core data only — price leads at `--text-xl` + verdict, title, address, mono data line, exception chips only (why/membership expanders + external links + status select live in the dossier, already present there). Progressive reactions: untouched card shows Like/Pass/Reject only; reason chips + Save appear after a verb tap; a saved decision collapses to a "✓ Liked — change" one-liner; Saved drops its redundant `<details>` edit wrapper; deck + dossier keep richer chrome and inherit the picker. All four owner decisions via AskUserQuestion recorded in the commit. Pins updated across property-card/listings-browse/saved-rejected suites. 980/980. *(2026-07-02)*

**Phase 4 — Intelligence engine (module-by-module per §10.0)** *(entered 2026-07-01 while ⚙ 3.1 design review is pending — Phase-3 builds 3.4–3.9 resume the moment it lands; expansion below from segments/10.6 + the 2026-06-19 log)*
- [x] 4.1 Interface pinned: `tests/contract/engine-interface.test.js` — every named export of the 9 refinement + 3 learned-preferences modules listed and asserted by static source parse (12 tests; additions free, removals/renames deliberate). 873/873. *(2026-07-01)*
- [x] 4.2 B3 ⚠ DONE: `fisherExactPValue` (one-sided hypergeometric tail, log-factorial memo, exact at any n) replaces the z-test feeding BH-FDR; runs on RAW counts (exact tests need integers — new raw pool totals in `scoreFromAggregates`; decay keeps driving Wilson/lift/p̂). Old grid recorded pre-swap (e.g. 3/4 vs 1/4: z said 0.079, exact truth 0.243 — the small-n anti-conservatism B3 named); new pins include hand-computed 17/70 and 2126/184756 tails + degenerate/fractional guards. Deliberate rename recorded in the 4.1 interface pin (consumer sweep: only engine tests). Live suggestions re-score on the next engine run (⚙ secrets). 874/874. *(2026-07-01)*
- [x] 4.3 B6 DONE: `scoreFromAggregates` result carries `gate_stats` (per dimension: total + pass counts for global/sample/confidence/disproportionality/persistence + qualified/actionable) and `planRun` records it into `refinement_runs.params.gate_stats` (existing jsonb — no migration). Unit pins: counts ≡ the per-candidate gate flags; run-row audit assertions. Calibration data accumulates from the first scheduled run (⚙ secrets). 875/875. *(2026-07-01)*
- [x] 4.4 B5 decision RECORDED (`04-program.md` §5b): KEEP the explainable five-gate core — the review's own trade-off + the trust north star; Fisher (4.2) was the cheaper statistical win, calibration audit (4.3) is the revisit trigger. *(2026-07-01)*
- [x] 4.5 P10a DONE: new `whySignalsFor(row, effective)` — the suggestion's own learned weight (`main`, direction liked/disliked) + the two strongest same-kind co-signals (`supporting`); `toCard(row, effective?)` carries `whySignals` AND appends a "Learned weight: −0.12 (disliked) — the model already leans this way" line to `whyLines`, so it renders in the existing Why? drawer with zero page-markup change. Threaded: `classifySuggestions(..., { effective })` ← `sources.js` (already had the weights); `fromEngineCard` passes it to the shared inbox shape. Interface pin extended. 877/877. *(2026-07-01)*
- [x] 4.6 P10h UX DONE: a `reconsider` probation row now headlines "Latest re-checks suggest this might be worth another look" with a one-tap **Re-enable this area** (same `bringback` storage path — deleting the probation row returns the area to the scraper's demand set next scheduled run, the cost-safe "re-probe now"); non-reconsider rows keep the paused copy. Threaded from `scrape_probation.status` per card; source-pinned + the view-layer reconsider label was already unit-pinned. 878/878. *(2026-07-01)*
- [x] 4.6b P10h detection DONE: the scheduled engine job now writes the reconsider hint — pure `scope#reconsiderUpdates` (genuine post-`approved_at` reactions per paused area; ≥ new `RECONSIDER_MIN_REACTIONS: 5` trials, pass = non-reject trial; rate < `RECONSIDER_RATE` → `active→reconsider`, reversed evidence walks it back; `restored`/non-area rows never touched) + `persistence#renderProbationSql` (FROM-status-guarded UPDATEs — concurrent bring-back no-ops) emitted by `refinement-run.mjs` (file + REST modes; aggregates-mode holds hints, no raw evidence). Notify-only holds: both statuses stay paused. Driver smoke-verified end-to-end; goes live with the daily job at ⚙ 2.16/4.9. 886/886. *(2026-07-02)*
- [x] 4.7 P10c DONE (deviation recorded: recompute-time, not the spec's per-reaction write — the append-only log stays the single source of truth, no §16 rewrite, no migration): `recomputeLearnedPreferences` now persists ranked attributed-reason counts under the reserved `derived.__reason_counts` key (`REASON_COUNTS_KEY`; inert everywhere — no numeric `.weight`; aggregated by the existing `trends-glance#reasonCounts`, no duplicate logic); new pure `view#topDislikesLine` renders "Your top dislikes: Too expensive ×7 · …" with the confidence meter from ONE prefs row (no log fetch — also the cheap read for future dashboard/Ask consumers). New integration suite pins the real recompute write via the core seam (counts + overrides/dismissals preservation); reserved-key inertness pinned in view + trends suites; interface pins extended; INTELLIGENCE_RULES + schema comment reconciled. Live row updates on the household's next retrain (portal-side, §18.4 hands-off). 891/891. *(2026-07-02)*
- [x] 4.8 P10i DONE: migration `refinement_runs_weights_snapshot` applied via `apply_migration` (approval stream back up — no execute_sql fallback needed this time) and column live-verified (jsonb, nullable); `planRun` carries `ctx.weightsSnapshot` onto the run row and `renderPlanSql` writes the column (NULL-stable shape); the driver flattens the live `learned_preferences.derived` via `effectiveWeights(derived, {})` (reserved `__reason_counts` + non-numeric entries drop out by construction) in both REST (`select=+derived`) and bundle (`learnedDerived`) modes. §18.2 freshness ran clean first (all tracked tables at snapshot high-water). Unit pins + driver smoke; schema.sql + REFINEMENT_README reconciled. Snapshots accumulate from the first scheduled run (⚙ 2.16/4.9). 893/893. *(2026-07-02)*
- [ ] 4.9 ⚙ Scheduled cadence goes live the moment the 2.16 secrets exist (refinement-run.yml already built — same owner action, no new work).
- [x] 4.10 Stryker apparatus LIVE (opt-in `npm run test:mutation`; @stryker-mutator/core devDep; command runner drives the unit tier; incremental re-runs enabled; never in the default gate). **Measured baseline 2026-07-02: 56.43% overall (3,603 mutants, 7m36s).** Reality-wins deviation from the ~75–80 blueprint number recorded: the statistical core already sits at 68–76 (weights 75.5 / scope 75.3 / engine 69.2 / persistence 68.2) and the drag is copy/render surfaces (trends-glance render half 23.0, signals label tables 34.2, view copy strings) where killing mutants = brittle string-pinning. `thresholds.break=55` is a RATCHET FLOOR (only ever raised), not the target. 893/893. *(2026-07-02)*
- [x] 4.10b Mutation-score ratchet DONE (2026-07-03, two commits): **56.43 → 74.86%** (3,281 mutants, 9m42s, `--force`), statistical core at/above the blueprint band — engine 85.0 / persistence 91.2 / weights 90.1 / radius 78.7 / radius-persistence 77.4 / trends-glance pure half 85.7. Four new `tests/unit/*-hardening.test.js` suites (+59 tests): exact-value Wilson/Fisher anchors, every tier/gate threshold at exact equality, the §2.8 tie-break ladder, BH-FDR edges, radius quantile/sector/waste boundaries, the FULL resolveStatus matrix (snooze-expiry boundary), metricsOf/paramsOf audit shapes, **byte-exact SQL goldens for all three rendered write batches** (executed SQL = behaviour), signals attribution tables + label vocab pinned verbatim (semantics, not copy), inference keyword grid (spaced "off street parking" abstention pinned as-is). Excise-vs-pin decided: trends-glance RENDER half excised in-source (`Stryker disable all` + reason; pure half stays scored); view/config/observations/search copy surfaces accepted at 41–66 as recorded headroom, not debt. `break` ratcheted 55→**73** (raise-only, just under measurement). Harness 1065/1065.

**Phase 5 — Finances (trust surface; every visible-number change flagged §3.10b)** *(expanded to atomic steps 2026-07-02 on entry, from segments/10.3 §"Refactor Opportunities"/"Suggested Sub-Phases" + the A-series external-validation findings + 04-program §4 rail row. Order: net first (5.1–5.3), then the two ⚠ visible-number corrections, then additive model growth, then presentation. NOTE: 5.9–5.11 overlap the Phase-3.8 finances-page rebuild — whichever lands first absorbs them; re-check before starting.)*
- [x] 5.1 Golden-master grid DONE (test-only): 153-row fixture (`tests/characterization/golden/calc-grid.golden.txt`) regenerated from the live modules each run and diffed line-by-line — SDLT every band edge ±£1 in both regimes, mortgage 3×4×4 principal/rate/term grid + 0%/fractional, LTV tier edges incl. 89.95→90 round-half-up, LISA £4k allowance + £450k cap edges + string coercion, savings rounding/`Infinity`, outlay grouping incl. unknown categories/non-numeric costs; plus fixture-independent hand-computed anchors (FTB £500k cliff = exactly +£5,000). Reality-wins correction recorded: the FTB cliffs are **£300k/£500k** (Apr-2025 regime) — this line's original "£625k cliff" was the pre-2025 regime. A §3.10b change regenerates the fixture DELIBERATELY in its own commit. 895/895. *(2026-07-02)*
- [x] 5.2 DONE — finance mutation scope live as its OWN config (`stryker.finance.conf.json`, `npm run test:mutation:finance`; separate from the engine config so each domain's ratchet floor is honest; kill vehicle = unit + characterization tiers). **First baseline 63.55%** exposed the real gap: the 5.1-covered pure calculators measured 79–100 but `affordability.js` — the VERDICT ENGINE — sat at **42.59** (182 survivors). Fold-back shipped as a second golden grid (`afford-golden-grid.test.js`, 41 rows): three profiles isolating each verdict driver against worst-of masking (LTI ladder straddling 4.5/5.5/6.0 + LISA cap + FTB cliff; payment% over 40/52/60; spare over 400/100/negative), input edges, the scenarios modeller, fixture-independent band-edge anchors. **Re-measure 87.07% overall / affordability 89.91 (survivors 182→32) in 1m19s; `break` ratcheted 50→86.** Process note recorded in the config: incremental caching can't see kill-vehicle changes under the command runner — always `--force` after strengthening tests. 938/938. *(2026-07-02)*
- [x] 5.3 B2 constants-drift rail LIVE (test-only): `tests/contract/intelligence-constants-drift.test.js` parses the `docs/INTELLIGENCE_RULES.md` literals against `intelligence-constants.js` — LTI/payment/spare band tables, LISA cap, LTV tier list, stress uplift, FIT_BANDS and all 13 documented FIT_WEIGHTS — and fails on ANY skew; a regex that stops matching fails loudly too (doc-structure change can't silently disable the rail). Verified red-on-drift with a mutate-and-restore round-trip; today's doc and code agree everywhere. 941/941. *(2026-07-02)*
- [x] 5.4 A5 ⚠ §3.10b DONE — **visible-number change (owner-pre-authorised via the approved plan):** "stress test" → **"rate-rise sensitivity"** relabelled at every render site (whyVerdict line, afford-widget "Rate-rise P&I" cell now labelled `@<rate>%` dynamically instead of the hardcoded `+3pp`, setup intro copy, INTELLIGENCE_RULES §renamed); mechanism modernised to **sensitivity rate = max(assumed + `RATE_RISE_UPLIFT_PP` 1pp, `RATE_RISE_FLOOR_PCT` 7.5%)** (FPC test withdrawn Aug 2022; MCOB reversion+~1pp with ~7–8% floor), both per-household overridable via `finances.mortgage.rateRiseUpliftPP/rateRiseFloorPct`; `STRESS_UPLIFT_PP` deleted. At the sample 5.35% rate the sensitivity rate DROPS 8.35%→7.50% (payment figure falls accordingly). New `rateRiseRatePct` output field (API names `monthlyPIStressed`/`stressedPaymentToIncome` kept — render labels were the scope). Afford golden grid regenerated DELIBERATELY (41/41 rows: new field + new sensitivity values); drift rail re-pointed at the new doc rule (uplift + floor both pinned); unit anchor pins the 7.5% floor binding. 941/941. *(2026-07-02)*
- [x] 5.5 A3 ⚠ §3.10b DONE (reality check first: the tile had NO LISA logic at all — it renders a synthetic today→+4mo sell-and-transfer seasoning bar; the wrong "4–5 year" rule lived only in the old spec, already dead. Live data confirmed via MCP: two S&S ISA rows, **no LISA row** — so **no visible change for this household today**; the correction is the rule made real for when a LISA exists). Shipped: pure `lisaUsableWindow()` in `calc-lisa.js` (§16 extend-only, declared) — **usable-from = first contribution + 12 months** per GOV.UK, explicit `firstContributionDate` beating the `accountOpened` proxy; the tile now renders that window (met/countdown captions) when `investments.lisa` exists, seasoning fallback byte-preserved; `getInvestments()` extended (§16 extend-only) to surface a `Lifetime ISA` row as `lisa` via a second query, with the primary query now excluding lifetime rows so a future LISA can never masquerade as the S&S blob. Mock client gains `ilike` + `not(...ilike)`. New unit grid (5 tests incl. tile source rail) + integration suite over the real read path via the core seam (3 tests; found+handled: Node ≥22's global localStorage made the write-through cache leak across tests — flushed via `_internal.removeLocal`). 949/949. *(2026-07-02)*
- [x] 5.6 A4 DONE (additive copy, zero number changes): the **£450k–£500k band** now renders its own sharper line — `whyVerdict` (FTB only: keeps SDLT relief, loses LISA bonus, 25% charge to use LISA funds; generic cap line returns >£500k / non-FTB) and the deposit-section LISA banner (same band branch); the "LISA rules under review (2026 consultation)" caveat now suffixes every LISA-banner branch; Ask facts (`prompt.ts`) gain the mismatch fact + never-present-as-permanent instruction (repo-side only — the ask function has NEVER been deployed; that whole admin handoff incl. `ANTHROPIC_API_KEY` remains the owner's queued step, so no deploy owed). Pinned: unit band tests both sides of £500k, golden anchors re-pointed (mismatch in-band, generic above), fixture regenerated deliberately (exactly the 2 band rows), Ask-facts source pin in `ask-tools.test.js`. 951/951. *(2026-07-02)*
- [x] 5.7 A7 DONE in one commit (both halves additive; A7's third item — leasehold running costs — was never in this step's scope and stays with the segment): **MGS/"Freedom to Buy"** modelled as constants (`MGS_LTV_MIN/MAX_PCT` 91–95, `MGS_PRICE_CAP_GBP` £600k) + `bandSignals.mgsEligible` + a `whyVerdict` high-LTV-route line when the assessed LTV sits in-window at an in-cap price (a low-deposit buyer sees an option, not a dead end); INTELLIGENCE_RULES §new + drift rail pins all three numbers. **Transaction-cost stack**: `TRANSACTION_COST_CHECKLIST` (7 named costs) + pure `missingTransactionCosts()` in calc-outlay (§16 extend-only) matching the household's free-text rows on item+notes; the finances one-time table now footers "Not yet itemised: …" via `#onetime-gaps` (sample household's only gap: broker fee). Unit window/cap/checklist tests + render source rail; golden regenerated deliberately (41/41: new signal). 954/954. *(2026-07-02)*
- [x] 5.8 DECIDED: **NO SPLIT** (reality wins, per the note recorded at the 3.8 expansion). `section-v3-charts.js` is 334 lines — *under* the §19 "split past ~400" norm the A1 finding predates; a shim + seven per-chart files would add indirection with no size need. Revisit only if the file grows past the norm (the §19 rule then applies mechanically). *(2026-07-02)*
- [x] 5.9 ABSORBED by 3.8c (2026-07-02, per the recorded overlap note): width-keyed chart heights → `dvh`; stale `.chart-tall` card clamp deleted; SVGs verified `viewBox` + `width:100%/height:auto` compliant; flow-bar ≤320px-safe; vw-keyed heights source-railed against return.
- [x] 5.10 ABSORBED by 3.8d (2026-07-02): all 12 chart surfaces `role="img"` + named + caption-described (deviation from the in-SVG `<title>/<desc>` letter recorded there — renderers `replaceChildren()` per draw; `aria-describedby` → the live verdict caption is the durable form).
- [x] 5.11 C1+E1 DONE — **C1:** `deriveFinances` now warns when the `investments` key is OMITTED (a forgotten record silently understates totalSavings); explicit `{ investments: null }` = deliberate cash-only, silent. All 7 production call sites already pass it (zero live warns); unit suite converted to the explicit contract + a warn-behaviour test. **onUpdate decision RECORDED** (comment at `page-finances#init`): criteria/investments stay read-once + re-read-on-save — single-user app, write-through cache refreshes next visit; revisit only if mid-session portal edits become a real workflow. **E1 audit CLEAN** (per its own spec: review, no new tests): every finances section HTML write either `esc()`s free text (renderTable escapes label/value/totals — the only path user free-text takes) or interpolates pure numeric formatter output (`gbp`/`toFixed`/pct); element lookups consistently null-guarded; no fixes owed. 955/955. *(2026-07-02)*
- [x] 5.12 **PHASE 5 COMPLETE** — exit done: §10.3 segment corrections appended (reality-wins deltas: A5 sensitivity mechanism, A3 no-LISA-row/no-old-logic double staleness, A4 never-deployed ask function, A7 item-3 leasehold stays open, £300k/£500k FTB cliffs, C1/E1); `plan/README.md` re-baselined. **Finance-mutation exit score: the 5.4–5.7 additions dipped the scope to 85.58 vs the 86 ratchet floor; exact-survivor killer tests restored it — re-measured 87.04% (642→657 mutants incl. affordability 89.97, calc-lisa 88.0; 1m36s, `--force`); floor stays 86 (raise-only, kept just under measurement).** Re-baseline: 959/959 across 100 suites; lint 5 justified fingerprints. *(2026-07-02)*

**Phase 6 — Areas content & data quality** *(expanded to atomic steps 2026-07-02 on entry, from segments/10.5 §Refactor Opportunities + reality checks. Verified today: the matched-price triplication is LIVE (page-areas.js:78, page-area-detail/sections.js:172, page-map.js:186 — divergences: sections.js also falls back to `area.prices`, map returns the bare price); priceSummary still bakes only in build-areas (sync `canonicalRecord` passes the stored DB value through — stale-able); the onboarding-stub literal sits in sync + parity test; content is ~89% done at the area grain (174/196 fully complete; remainder = 3 directory (worthy-down-so21, ebble-sp5, newtown-sp5) + 1 partial (lane-end-so21) + 16 missing `schools` + 5 missing `placesToEat` — empty array counts as missing, so school-less hamlets need nearest-school entries per the dossier convention, or an honest decision). §18.2 freshness clean (areas high-water 2026-06-28 = snapshot, reconciled 07-01). Dispositioned elsewhere, not re-planned here: map a11y + coord-quality VISUAL cues → post-refactor visual pass (3.7c); drawn-zones→Supabase → Phase 9; Leaflet KEPT (ADR 04-program §5b); zoom-perf clustering → visual pass (real-device concern).)*
- [x] 6.1 Matched-price ONE HOME DONE: pure `assets/js/areas/matched-price.js` (unified source `priceSummary || prices` — behaviour-preserving superset: index-shaped areas carry no `prices`; returns `{price,label}`); the three copies deleted — page-areas + page-map import directly (map call site takes `.price`), sections.js imports + re-exports so the coordinator's import surface is unchanged; unit grid (10 tests) pins the preference walk, Bungalow→avgDetached, cheapest-available fallback order, priceSummary-beats-prices, null/no-data handling + a source rail (no `PROP_TO_KEY` outside the module; page-areas/page-map may never walk avg* keys again — sections.js keeps its legitimate prices-section RENDER labels). Fallback-bias decision RECORDED in the module header (keep cheapest-available — changing it changes visible verdicts, §3.10b; revisit only with the owner). 969/969. *(2026-07-02)*
- [x] 6.2 priceSummary ONE BAKE DONE: `bakePriceSummary(prices)` exported from `tools/area-fields.mjs`; sync's `canonicalRecord()` now DERIVES it (the stored DB value is ignored — stale-proof at step 0, and the sync↔build flip-flop noise a drifted DB summary would cause is impossible); build-areas' inline bake deleted for the same call. Verified: `build-areas` run → zero file drift (bake byte-identical). New unit suite (8 tests): derivation grid (partial fills, non-summary keys avgSold12Mo/avgBungalow don't trigger, empty→null), canonicalRecord stale-value pins (exported for the seam), the repo-wide derived-state invariant (every committed per-area file's priceSummary ≡ bake(prices) — the offline form of the idempotence contract), and a source rail (both tools use the shared bake; the inline copy + pass-through can't return). 978/978. *(2026-07-02)*
- [x] 6.3 Stub predicate unified DONE: `isOnboardingStub()` exported from `tools/area-fields.mjs` (record-shaped — DB-row callers pass `row.data`); sync-areas-from-supabase filters by it and areas-db-repo-parity imports the same export — the duplicated literal is dead in both. New `tests/unit/onboarding-stub.test.js` (5 tests): behaviour grid (source is the discriminator, active irrelevant; curated/absent-source/null safe; row-vs-record shape pinned) + source rail (both files must import the shared predicate; an inline `household-onboarding` comparison or local redefinition can never return). Browser-side `area-ref.js` predicates are a separate page-layer concern, untouched by design. 985/985. *(2026-07-02)*
- [x] 6.4 Completeness surfacing DONE: the completeness rule moved ONE-HOME-style to browser-safe `assets/js/areas/completeness.js` (CONTENT_FIELDS/getField/completeness/deriveStatus verbatim; `tools/area-fields.mjs` re-exports so area-status.mjs + the archive migrator import unchanged — pages must not import from tools/, and .mjs-over-Pages MIME risk avoided); new pure `researchStatusLine()` (null when complete; "Research in progress — N of 12 sections researched."; "Not yet researched — every section below is a placeholder." at 0) rendered by page-area-detail into a quiet `.research-status` note under key-facts (textContent only; `--paper-2` tint per DESIGN.md §3 no-left-border-pill rule; hidden ships default so a complete dossier never shows it). New `tests/unit/area-completeness.test.js` (8 tests: behaviour grid, re-export IDENTITY, cue contract, source rails) + a page-DOM pin (slot position + ships hidden/empty). Visual-polish grade deferred to the post-refactor visual pass as scoped. 994/994. *(2026-07-02)*
- [x] 6.5 Content batch A DONE (across two sessions — the first died post-DB-write on worthy-down, pre-materialise; recovered + completed 2026-07-02): all 4 incomplete areas researched per §7 and written DB-first. worthy-down-so21 recovered from the surviving DB write (jsonb-equality-verified transcript → materialise); lane-end-so21 spot-checked (Ship Inn verified trading 2026) partial→researched; ebble-sp5 honestly recorded as the OS locality label for the Ebble/Chalke valley (NOT a settlement — verificationNotes recommends deactivation, owner decision pending, mirroring worthy-down's garrison treatment); newtown-sp5 researched as the west-of-Avon arm of Downton (Ofsted-cited schools, named pubs, Downton £406k sold-price context). Every write MCP UPDATE + re-SELECT jsonb-equality verified → sync → build → verify-coords (196/196 corroborated offline; online reverse-geocode honestly skipped — postcodes.io is CI-only) → harness green. **All 196 areas now status=researched at the area grain**; the field-gap sweep (16× schools, 5× placesToEat) is 6.6. *(2026-07-02)*
- [x] 6.6 Content batch B DONE + **Phase 6 COMPLETE** (2026-07-02): the full missing-field sweep — 16× `schools` + 5× `placesToEat` across 19 areas, every entry the researched nearest-school/nearest-pub per the dossier convention (no none-nearby cases arose: every area has a real school/pub within ~6 mi). Web-verified per cluster: Four Marks/Ropley CE (kitwood, west-tisted), Preston Candover CE + Perins (northington, ovington via Cheriton), Dever-group primaries (stoke-charity, wonston, west-stratton), Medstead CE (upper-wield), St John's Tisbury (lower-chicksgrove), Wellow Primary (plaitford-green), Wilton CofE (south-newton, stoford, ugford, little-wishford), Wylye Valley CE Codford incl. the Steeple-Langford-site-closed-2010 fact (stapleford, little-langford); pubs: Bell Inn Wylye (sherrington), Green Dragon Alderbury (west-grimstead), Downton pubs (witherington), Royal Oak Great Wishford (little-langford, little-wishford). Written as surgical jsonb patches (schools/placesToEat + sources) via MCP UPDATE…FROM(VALUES), re-SELECT shape-verified 19/19, materialised (sync 0-drift), `area-status --missing` now EMPTY — **196/196 researched, zero missing fields**. Phase-6 exit recorded: §10.5 corrections appended (refactor §1–3/§5/§6 shipped-or-closed, freshness bullet retired, content re-baseline). Imagery stays queued in docs/CHECKLIST.md. *(2026-07-02)*

**Phase 7 — Ask assistant** *(expand on entry)*
**Phase 7 expanded on entry (2026-07-03, §0.2 mode-2 — reality-checked against today's code + the released Claude 5 family):** *P1's premise is INVALIDATED — `claude-fable-5` shipped as the PREMIUM tier ($10/$50 per MTok, ~10× Haiku; always-on thinking; refusal-fallback plumbing; 30-day-retention requirement), not the assumed "new cheaper tier". Owner decision (2026-07-03, AskUserQuestion): default Ask to **`claude-sonnet-5`** ($2/$10 intro to Aug 2026, then $3/$15; near-Opus tool use), keep Haiku 4.5 as the manual step-down; deploy via MCP. F2's caching worry is also stale: the breakpoints on the static+compose system blocks ALREADY cache tools+system together (render order tools→system), and that prefix (~6–7K tokens) clears every model's minimum — the fix is verification, not relocation. E4's JWT forwarding is ALREADY the architecture (index.ts builds the RLS-scoped client from the caller's Authorization header; executors add belt-and-braces household_id filters). P6's full mock-Anthropic E2E needs a connected host — dispositioned to the needs-network queue; the offline rail below covers the drift surface.*
- [x] 7.1a Model default → `claude-sonnet-5` (owner-decided): index.ts DEFAULT_MODEL + ALLOWED_MODELS (sonnet-5 + haiku-4-5 step-down + sonnet-4-6 transition compat for cached clients); explicit `thinking:{type:"disabled"}` on sonnet-5 (adaptive is ON when omitted there — silent default change — and would eat the 1024-token brevity budget); compose.js COMPOSE_MODEL → sonnet-5; docs/ASK.md re-baselined.
- [x] 7.1b Strict tool use extended (F3): `strict:true` + `additionalProperties:false` + explicit `required` on 12 of 13 tools; `get_outreach_brief` is the DOCUMENTED exception (its `extra` input is deliberately free-form — strict schemas cannot carry an open object).
- [x] 7.1c Prompt versioning: PROMPT_VERSION exported from prompt.ts, logged per request; static+compose block hashes pinned so any prompt edit without a version bump fails the rail.
- [x] 7.1d Tool-contract rail (04-program §4): new tests/contract/ask-tool-contract.test.js — pins the 13 tool names, the strict/additionalProperties invariants (incl. the 7.1b exception), TOOL_LABELS coverage in transcript.js, ALLOWED_MODELS/DEFAULT_MODEL, and the prompt-version↔hash ratchet.
- [x] 7.1e Sanitisation audit (P2): injection cases pinned in tests/contract/ask-tools.test.js — buildListingsQuery's PostgREST `.or()` expressions must stay structurally closed under hostile area/keyword input (sanitizeFilterTerm strips `,()*%\`); the "PostgREST parameterizes values" assumption documented.
- [x] 7.1f DONE (2026-07-03): harness 1006/1006 → committed + merged → `ask` v6 deployed via MCP (ACTIVE, verify_jwt on, entrypoint ask/index.ts; no boot errors in the edge logs). OWNER ACTION: run the docs/ASK.md browser smoke test (one question on the ask page), then confirm the usage log line shows model=claude-sonnet-5, prompt=2026-07-03, thinking=0 and cache_read>0 on the second turn. Rollback = redeploy the previous commit's four files.

**Phase 8 — Profile, criteria, journey** *(expanded on entry 2026-07-03, §0.2 mode-2 — reality-checked against live code. The segment's 2026-06-16 smells are half-void: the setup WIZARD is gone (steps/autosave/completeness/validate survive as the shared field engine behind `forms/field-renderer.js`), the outreach GRID is gone (Ask Compose authors outreach; pure `outreach-renderer/store` helpers survive), and the "three profile modules" already shrank to thin field-engine-driven pieces (page-profile 249 / page-profile-detail 94 / page-profile-page 48 + page-criteria 381). What genuinely remains: profile.html loads FOUR sibling page modules where §19's norm is ONE thin entry per page; `dataNeeded` is hand-written per template with no rail against the `{{placeholders}}` actually in the bodies; journey.json's data-class needs a recorded decision; the field-engine's branch matrix (who sees which fields) is unpinned; Ask-Compose outreach a11y unaudited since the grid died.)*
- [x] 8.1 ONE profile entry DONE (§19 norm): `page-profile-page.js` is the page's single coordinator — it imports the three siblings in the legacy `<script>` order (page-profile → page-criteria → page-profile-detail; ES import order preserves execution order, so behaviour is byte-identical); `profile.html` drops 4 page scripts → 1. New pin in `tests/pages/profile.test.js`: exactly one `page-*` script on the page + the coordinator's import order at source. 1066/1066. *(2026-07-03)*
- [x] 8.2 Template↔dataNeeded rail DONE: per-template contract test (24 tests) parses every `{{path}}` + `{{#if path}}` from subject+body and asserts exact parity BOTH ways — an undeclared used path (silent blank in a real email) and an unused declared path (stale over-gating) both fail; a stale parser regex fails loudly (zero-placeholder guard). Dry-run found **zero drift today** — the hand-written lists are accurate; the rail locks that in. 1090/1090. *(2026-07-03)*
- [x] 8.3 journey.json data-class DECISION recorded (2026-07-03): stays **repo-only content** (the checklists/outreach-templates class) — single-editor curated content, code-reviewed changes, tick-state already in Supabase `journey_progress`; a `journey_phases` table adds sync surface for zero owner benefit. Dated rationale in `docs/SUPABASE_SYNC.md` §0 + segment 10.8 corrections; revisit trigger = a real multi-editor/portal-edit need. Supersedes the segment's 2026-06-16 'move to Supabase' phase-4 recommendation. No table, no loader change.
- [x] 8.4 Field-engine branch matrix pinned DONE: `tests/unit/field-engine-branch-matrix.test.js` (6 tests) sweeps every buying-situation × employment-basis combination — income/mortgage step inclusion (cash-buyer ONLY exclusion), the exclusive employed/self-employed work sub-branches, earned/pension/joint income fields per combo, joint-application reveal (2 and '2' alike), the FTB-only LISA field, cash-buyer pension hiding, and the consent-gated health notes (control itself always visible — withdrawable, UK GDPR). Stale-hidden-value behaviour DOCUMENTED as observed-and-accepted in the suite header (hide ≠ purge; autosave writes dirty paths only — flip back and answers return; purge-on-hide would be a deliberate product change). 1096/1096. *(2026-07-03)*
- [x] 8.W OWNER-DIRECTED INSERTION (2026-07-03, mid-Phase-8 + inbox screenshot): **GitHub Actions audit + tidy-up** (§16 rail phase, owner-authorized in the request). FOUND+FIXED the email flood's root cause: ci.yml + pages.yml ran Node 20, but the 7.1d ask-tool-contract test loads the edge function's TS via `--experimental-strip-types` (Node ≥22.6) — every push since the Phase-7 merge failed CI AND blocked the Pages deploy (test job gates deploy). Both now Node 22 (annotated why) and green. Deprecation debt cleared: checkout@v4→v5, setup-node@v5, Node 20→22 across ALL workflows (runners force Node 24 on v4 actions with warnings). Honest cadence: review-counts cron */15 → hourly-offset ('7 * * * *') — measured reality was ~hourly (GitHub throttles sub-hourly crons) and the fetch-chasing workflow_run covers the moments that matter. REMOVED (git history keeps both): suzanne-backfill.yml (one-off standing-stock pull, now expressible as fetch-listings dispatch: new `area_ids` input + AREA_IDS env, already supported by the fetcher) and postcodes-accuracy.yml (dormant diagnostic; the tool stays in tools/). Verified working-as-designed, kept: fetch-listings (slot gate + pg_cron primary), radius-tune (daily, applies for real), review-counts (succeeding), refinement-run + remembership (honest no-ops pending ⚙ 2.16 — evaluate/apply steps skipped, quiet), resolve-areas + import-apify-runs (manual/dispatch machinery). All 9 survivors YAML-parse; harness 1096/1096 incl. the remembership contract pins. *(2026-07-03)*
- [x] 8.5 Outreach-via-Ask a11y pass DONE (audit + one fix + pins): the Compose dialog was already §11-clean — named native dialog, label-wrapped radios/fields, legends, `aria-labelledby` chip group; the drafted message is announced by design (it streams into the transcript whose assistant bubbles are `aria-live=polite` per transcript.js, and the draft card carries its own `role=status` region for copy/log feedback). FIXED the one gap: `#ask-compose-address` (free-text address, shown when no listed property fits) had only a placeholder — now `aria-label`'d. Pinned in `tests/pages/ask.test.js`: every compose control programmatically labelled (wrapper/for/aria-label/labelledby), the draft template's live region, the transcript's live-bubble mechanism at source. 1097/1097. *(2026-07-03)*
- [x] 8.6 **PHASE 8 COMPLETE** — exit done: §10.8 "Phase 8 exit corrections" appended (one-entry supersedes the three-page prose + its unowed submodule-merge proposal; stale module counts fixed — 249/94/55/381, all ≤400; coupling #6 upgraded to the 8.2 two-way rail, 24 templates not "30+"; 8.4 hide≠purge documented as accepted, retiring the wizard-era no-undo risk; 8.5 retires the pre-fold outreach a11y concerns; 8.W pointer → segment 10.10). The 2026-07-01 wizard/grid-gone corrections were already in place — verified, not duplicated. `plan/README.md` status block re-baselined for cold resumes (Phases 1–8 complete, harness 1097/1097 across 104 suites, current phase 9). *(2026-07-03)*

**Phase 9 — Backend/storage resilience** *(expanded on entry 2026-07-03, §0.2 mode-2 — reality-checked against live code. The segment's 2026-07-01 scan corrections hold, but two R-items have moved since authoring: E1 is MOSTLY SHIPPED (`supabase-client.js` already carries an `sb_publishable_*` key, not a legacy JWT — what remains is online verification that the legacy keys are revoked and the edge function's key handling is secret-model); R5's premise is STALE (`sync-state.json` is 117 lines, not "5000+" — the split shrinks to moving the `_note` archaeology out). NEW FINDING at entry, worse than the segment's R2 framing: `_save`'s fire-and-forget doesn't just lose the remote write on failure — the next `_get` revalidation CLOBBERS the newer localStorage value with the stale Supabase row (`core.js:158–164`), silently reverting the user's edit on screen. R2 therefore lands as journal+clobber-guard first, retry-drain second. R1 (TS migration of storage/) stays out of scope — zero-build site; the tier-0 `checkJs` ratchet + 9.7 generated types are the type net. All storage edits are §16 extend-only; execution steps below touch data/schema → §18.2/§18.3 ceremonies apply to those sessions.)*
- [x] 9.1 Failed-write journal + clobber guard DONE (R2a): `core.js` extended — a failed or session-less `_sbUpsert` journals `{table, value, queuedAt}` under `pending-writes` (latest per table); `_get` revalidation skips the cache overwrite (and `onUpdate`) while that table has a pending write, closing the silent-revert path; a later successful write clears the entry; `readPendingWrites` on `_internal` + sibling exports for the 9.2 drain. `_toast` gained a no-DOM guard (the failure path had never run under Node). New `tests/integration/storage-pending-writes.test.js` (6 tests) through the real core paths via `__REC_TEST_SB__` + the mock's new `failWrites` option; the no-journal revalidation branch pinned as-was. 1103/1103. *(2026-07-03)*
- [x] 9.2 Retry drain DONE (R2b): `drainPendingWrites()` in `core.js` — runs at boot (after `_initSb`) and on the browser `online` event; single-flight; per-table journal re-read mid-drain so a newer journalled edit wins (blob semantics); success toast when everything lands, error toast with the remaining count otherwise. DEVIATION (recorded): verification is the upsert ack (success clears the entry, failure re-journals — journal-empty IS the ack), not the planned deep-equal re-read, because Postgres jsonb does not preserve key order — a stringify comparison would false-mismatch and re-journal forever. 4 new integration tests (offline→online round trip, still-failing keeps entry, single-flight, empty no-op). 1107/1107. *(2026-07-03)*
- [x] 9.3 Ask-conversations cache DONE (R3, re-scoped): `listAskConversations(onUpdate)` is now stale-while-revalidate over the id/title/updated_at index (`ask-conversations-list` key) — cached list renders instantly, background revalidation fires `onUpdate` on divergence; `_fetchConversationList()` returns null on failure so a failed fetch can never clobber the cache with `[]`; create/rename/delete invalidate the cached index (no ghost rows after `refresh()`); message BODIES stay live-fetch by design (unbounded rows — recorded in the module header). `ask/history.js` split `render()` out of `refresh()` and passes it as `onUpdate`. New `tests/integration/ask-conversations-cache.test.js` (6 tests). 1113/1113. *(2026-07-03)*
- [x] 9.4 Snapshot split DONE (R5, re-scoped): `sync-state.json` now carries current high-water marks + one-line caveats only (the multi-KB archaeology is gone); every historical `_note` moved VERBATIM to the new `docs/archive/sync-changelog.md` (per-table sections, append-here convention in the header); a top-level `_note` records the convention, and the `docs-consistency` rail's tracked-table derivation now excludes underscore-prefixed metadata keys; `docs/SUPABASE_SYNC.md` §0 gains the convention line. Freshness tool + sync suite verified unchanged in behaviour. 1113/1113. *(2026-07-03)*
- [x] 9.5 One schema truth DONE (R6): live `list_migrations` reconciled — **32 migrations** (20260525214013→20260702085056), archive slices spot-matched to their migrations (`phase5_expanded_data_model`, `listings_l1`, `listing_reactions_multi_reason`, `household_feed_rpc`; reports add/drop cancel out), and `schema-live-feed-stats.sql` confirmed as the ONE known out-of-history `execute_sql` application (recorded; a second is a stop-and-reconcile). Every `.sql` under `supabase/` now opens with a `-- REFERENCE ONLY` marker naming its migration; `schema.sql`'s stale "Run in Supabase → SQL Editor" instruction (contradicting §18.5) replaced. New rail `tests/contract/schema-reference-only.test.js` (3 tests: slices present, marker mandatory, no dashboard-run instructions). README gains the rule + reconciliation record. 1116/1116. *(2026-07-03)*
- [x] 9.6 Key-model verification DONE (E1 ⚠ + E2): verified live via MCP — client key is the modern `sb_publishable_*` (live+enabled, repo matches); **E2 RLS sweep CLEAN** (`pg_tables WHERE NOT rowsecurity` → zero rows) and now baked into the §18.2 ceremony as sync-check step 1; no secret key committed anywhere (`backfill-content-direct.mjs` reads service_role from env; the ask function is anon-key + forwarded-caller-JWT with belt-and-braces executor filters). FINDING: the **legacy anon JWT key is still ENABLED** — and the platform injects it as the `SUPABASE_ANON_KEY` the ask function reads, so disabling it today would break Ask. ⚙ NEW OWNER ACTION (dashboard-only, no MCP tool): (1) point `ask` at the publishable key via a function secret + redeploy + smoke test, (2) then disable legacy JWT keys. Exact steps + findings recorded in `docs/SUPABASE_SYNC.md` §7 (new); §5's stale "Edge functions — none today" fixed. 1116/1116. *(2026-07-03)*
- [x] 9.7 Generated types DONE (R4/E4): `types/supabase.d.ts` generated from the live schema via `generate_typescript_types` (dated header, DO-NOT-HAND-EDIT); `storage/ask.js` annotated with JSDoc typedefs over the generated `ask_conversations` Row/Update types (extend-only — incl. the previously-untyped `saveAskConversation` patch object) and added to the tier-0 ratchet. Two support files: `types/cdn-modules.d.ts` (ambient declaration for the jsdelivr supabase-js URL import tsc can't resolve — pulled into the program via `supabase-client.js`) and `types/supabase.js` (empty runtime stub so the JSDoc `import('../../../types/supabase.js')` specifier satisfies BOTH tsc — which prefers the sibling `.d.ts` — and the asset-links rail's existsSync check, which scans specifiers inside comments too). Regeneration recorded as a schema-change ceremony step in CLAUDE.md §17 step 2 + `docs/SUPABASE_SYNC.md` §2.2 (applies to EVERY migration, not just new tables). NOTE: a prior session's first attempt was lost to a credit stop before commit; redone from scratch this session. 1116/1116. *(2026-07-03)*
- [ ] 9.8 Multi-device conflict strategy (R7 ⚙, own gate): prepare the recommendation — formalise last-write-wins + the 9.1 clobber guard as the documented strategy (single household, low concurrency); version-column conflict detection as declined-unless-need. One owner ask when reached; do not build until ruled.
- [ ] 9.9 Phase-9 exit: §10.9 segment corrections appended (E1/R5 reality, the clobber-guard finding, this expansion's outcomes); `plan/README.md` re-baselined.

**Phase 10 — Process & rails re-baseline** *(expand on entry)*
- [ ] 10.1 `docs/adr/` + template with every §4 rail change mapped (G2); commitlint in CI (G3); ⚙ branch protection + required CI on `main` (G1); new rails live (spend, tool-contract, RLS); CLAUDE.md §16/§6 + DESIGN.md re-baselined to the rebuilt system; final §2.7 leanness sweep.
