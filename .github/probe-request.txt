# probe-request — fallback inputs for .github/workflows/probe-rightmove.yml.
#
# Since 2026-09-04 (docs/adr/0012) pushing this file fires NOTHING. The probe
# workflow runs only from the Actions tab "Run workflow" button; the values below
# fill in any dispatch input left blank. Editing this file is never a run.
#
# probes=dryrun   ZERO SPEND. Dry-runs the profile lane against live data: profiles
#                 load, areas resolve, targets build, URLs are constructed — and
#                 fetch-listings.mjs returns before any Apify call.
# probes=H        free (actor schema + recent runs' real charges)
# probes=P        free (account state + the exact 403 body the fetcher hides)
#        I  ~$0.02  does a valid zero-result search cost anything
#        J  ~$0.10  monitoringMode — does run 2 bill only new listings
#        K  ~$0.05  does fullPropertyDetails:false return gate-able text
#        A  ~$0.05  does an exact minPrice==maxPrice band work
#        F  ~$0.10  are two adjacent outcodes disjoint
#
# Keep this on a free/zero-spend value unless a paid probe is deliberately intended.
probes=dryrun
outcode=SP11
outcode2=SP4
price=400000
profile_ids=528095ca-3072-4542-bb7f-4b2b78f9828b,c39c038a-8068-479a-84c7-adbc3fe49a47

# run: 2026-08-30 PLAN_ONLY profile-lane plan (genuinely zero spend)
